// ExternalDrawingLibrary.js
export default class ExternalDrawingLibrary {
    renderCircle(centerX, centerY, diameter) {
        console.log(`Đang vẽ hình tròn tại (${centerX}, ${centerY}) với đường kính ${diameter}`);
        // Thêm mã thực tế để vẽ hình tròn
    }
}
