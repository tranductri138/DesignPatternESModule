import Document from './document.js';

export default class RealDocument extends Document {
    constructor(content) {
        super(content);
    }

    // Có thể thêm các phương thức cụ thể cho RealDocument nếu cần
}
